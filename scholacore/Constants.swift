//
//  Constants.swift
//  scholacore
//
//  Created by Tarun kaushik on 29/05/17.
//  Copyright © 2017 Tarun kaushik. All rights reserved.
//

import Foundation
import Firebase

/*
struct Constants {
    
    struct dataBaseReference {
        static let userProfile = FIRDatabase.database().reference().child("Profiles") // profiles
        static let postsRef = FIRDatabase.database().reference()// posts
        static let NotificationRef = FIRDatabase.database().reference().child("Notification")// notifications
    }
    
    struct storageReference {
        static let userProfileImage = FIRStorage.storage().reference().child("ProfileImages")// profileimages
    }
    
    struct coreData{
        static let context = (UIApplication.shared.delegate as! AppDelegate).persistentContainer.viewContext
        static let appDelegate = UIApplication.shared.delegate as! AppDelegate
        static let saveContext = (UIApplication.shared.delegate as! AppDelegate).saveContext()
    }
    
}
*/
